use a last in first out indexed array to track free spots for when components are expired

if there are no free spots to use in free location index, use largest last location index and addvance the value by one





non-linear array smartStorage manages stores the actual data for a component
an int list saves the component index in the smart storage array, allowing for quick updating of all components
index of entity on the non-linear heap storage is id
use a bitmask component to identify the attached components to an entity
make a list of ints for each entity archetype; allowing for fast access to each entity type.
?make function to generate entity lists or destroy entity lists depending on what archetypes there are?
make function to add add entity id to each list that it can be classified according to
make a way to access the correct archetype list based on the bitmask archetype of the entity.
